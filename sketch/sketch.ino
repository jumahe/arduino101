// ICI importez les bonnes librairies

// ICI déclarez vos variables et constantes
// - l'URL du service
// - la query (qui sera à concaténer à la suite de l'URL)
// - un client HTTP (pour interroger le service)
// - la pin de la LED
// - le buffer de données réceptionnées
// - des variables complémentaires selon vos besoins

// SETUP (appelé une seule fois)
void setup()
{
  // initialisez le Serial
  // initialisez le Bridge
  // initialisez la pin de la LED
}

// LOOP (appelé en boucle)
void loop()
{
  // RAZ du buffer
  // envoi de la requête
  // lecture de la réponse (caractère à caractère)
  // traitement de la réponse
  // notification ?
  // temporisation (une pause avant la prochaine requête)
}
